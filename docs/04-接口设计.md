# 智枢·智能体平台 接口设计（API）

版本 V2.2 · 2026-09-04 · 以 `backend/app/api/v1/` 为准

> 本文档是前后端与外部调用方的契约。改接口必须同批更新：路由代码、`frontend/src/api/index.ts`、本文档。
> 标注"规划"的小节是已定稿但尚未实现的契约，实现时按此执行，不得另起一套。

## 1. 基础约定

| 项 | 约定 |
|---|---|
| 前缀 | `/api/v1`；健康检查 `GET /health` 无前缀 |
| 协议 | REST + JSON；文件上传 `multipart/form-data`；对话回复 SSE |
| 鉴权 | `Authorization: Bearer <JWT>`；除登录与健康检查外全部需要 |
| 字符集 | UTF-8 |
| 时间 | ISO 8601 带时区字符串，例 `2026-09-04T03:57:56.476194+00:00` |
| 追踪 ID | 每个响应都带 `X-Request-Id` 头；调用方可用同名请求头指定（限 `[A-Za-z0-9._-]{1,64}`，非法值被忽略并另行生成） |
| 交互式文档 | FastAPI 自带 `/docs`，生产环境应关闭 |

## 2. 响应结构

### 2.1 成功

直接返回数据体，不套信封：单个资源返回对象，列表返回数组。

```json
{"id": 1, "name": "客服助手", "status": "published"}
```

三类例外，历史沿用，不再扩散到新接口：

- 删除类接口返回 `{"code": 0, "message": "ok"}`。
- 模型连通测试、工具测试返回 `{"code": 0, "message": "ok", "data": {...}}`。
- 上传文档返回精简对象 `{id, kb_id, name, file_type, status}`。

### 2.2 失败

HTTP 状态码表达错误类别，响应体固定 `{"detail": ..., "trace_id": "<X-Request-Id>"}`。

```json
{"detail": "工作流不存在", "trace_id": "1de7aa9bfeef4a18"}
```

| 状态码 | 含义 | detail |
|---|---|---|
| 400 | 业务前置条件不满足 | 字符串，可直接展示，如"模型不可用" |
| 401 | 未登录或 token 失效 | 字符串；前端收到后清除凭据并跳登录 |
| 403 | 无权限、账号停用、智能体未发布、来源 IP 命中黑名单（detail 固定"来源 IP 被拒绝"，由中间件在路由之前返回，`/health` 不受影响） | 字符串 |
| 404 | 资源不存在或不属于当前用户 | 字符串 |
| 409 | 冲突：重名、被引用无法删除 | 字符串 |
| 422 | 请求体校验失败 | FastAPI 生成的数组，逐字段说明（此响应无 `trace_id`，追踪 ID 见响应头） |
| 429 | 登录失败次数过多、API Key 配额用尽、入口限流（按 API Key / 登录用户 / 登录接口来源 IP 每分钟计数） | 字符串；限流的 429 另带响应头 `Retry-After`（秒）、`X-RateLimit-Limit`、`X-RateLimit-Remaining: 0` |
| 500 | 未捕获异常 | 固定文案"服务器内部错误，请携带 trace_id 联系管理员"；堆栈只进服务端日志，不外泄 |
| 503 | 模型熔断中：该模型连续失败达 `MODEL_BREAKER_FAIL_THRESHOLD` 次，`MODEL_BREAKER_OPEN_SECONDS` 内不请求上游直接失败 | 字符串"模型「x」暂时不可用（熔断中，N 秒后自动重试）"，带 `Retry-After`；对话接口以 SSE `error` 事件下发同一文案，工作流运行记录的 error 含同一文案 |

业务异常由服务层抛 `BizError(status_code, detail)`，全局处理器转成上述结构。没有独立的业务错误码，前端按 HTTP 状态码与 detail 文案处理。

`trace_id` 与响应头 `X-Request-Id` 是同一个值，也是服务端日志里 `[...]` 的那一段：拿它 grep 日志即可取到该次请求的全部记录。前端 axios 拦截器会把 5xx 的 `trace_id` 拼进错误提示，用户截图即可定位。

通过鉴权且未超限的请求，响应头带 `X-RateLimit-Limit` 与 `X-RateLimit-Remaining`（本自然分钟剩余次数），调用方可据此自适应；`/health` 与未鉴权的响应没有这两个头。

### 2.3 分页（FR-022）

除会话消息外，所有列表接口都是服务端分页，参数与响应结构统一：

```
GET /runs?page=1&page_size=20&status=failed&run_type=chat
```

| 参数 | 说明 |
|---|---|
| page | 从 1 起，默认 1 |
| page_size | 默认 20，上限 100，超出按 100 |
| 其余 | 各接口自己的筛选字段，全部在服务端生效 |

```json
{"items": [...], "total": 1234, "page": 1, "page_size": 20}
```

默认排序为 `id` 倒序（运行记录、审计、API Key、定时任务、会话按更新时间）或 `id` 正序（配置类），都带 `id` 作副键保证翻页稳定。空结果 `items` 为 `[]`。筛选参数全部在服务端生效，枚举型参数取值非法返回 422。

**排序参数**（页面深度优化，2026-09-06）：列表接口接 `sort`（字段名，各接口说明里列白名单）与 `order`（`asc` / `desc`，默认 `desc`）。`sort` 不在白名单 → 400 "不支持的排序字段：x，可选 …"；`order` 非法 → 422；`sort` 为空走默认排序。任何排序都自动追加 `id` 同向副键。

**时间区间参数**：按被过滤的列命名 `<列>_from` / `<列>_to`（运行记录 `started_from / started_to`，审计与文档 `created_from / created_to`），语义左闭右开 `[from, to)`，值为 ISO 8601 **必须带时区**（`2026-09-06T00:00:00+08:00`），不带时区 → 400 "…必须带时区"，`from >= to` → 400。

**批量接口**：`POST /<资源>/batch`，请求 `{"ids": [...], "action": "..."}`，`ids` 1～100 个（自动去重，空或超限 422），`action` 各资源白名单（非法 422）。语义是**逐条独立执行、逐条独立返回**，不是全成功或全失败：响应固定 200 `{"succeeded": [1, 2], "failed": [{"id": 3, "detail": "该模型已被 1 个智能体引用，无法删除"}]}`，单条的 404 / 409 / 400 都进 `failed`。各资源的 `action`：agents `publish / delete`；models、tools、api-keys、schedules、users `enable / disable / delete`；workflows、knowledge-bases、prompt-templates `delete`；documents `delete / reprocess`。

会话消息 `GET /conversations/{id}/messages` 是唯一例外：对话页需要整段历史，返回数组。前端下拉框取 `page_size=100` 的第一页（`OPTIONS_PAGE`），超过 100 项时要改为带 `q` 的服务端搜索，见演进路线。

### 2.4 ID 类型

主键以 JSON 数字传输。前端按 `number` 处理，比较用 `===`。平台内部 ID 远小于 2^53，暂不改为字符串；如引入雪花 ID 等长整型，必须全链路切换为字符串。

## 3. 认证与鉴权

### 3.1 JWT

`POST /auth/login` 返回的 token 有效期由 `ACCESS_TOKEN_EXPIRE_MINUTES` 决定（默认 1440 分钟）。payload：`sub` 用户 ID 字符串、`role`、`exp`。每次请求都会查库确认用户存在且启用，停用立即生效。

### 3.2 角色

| 依赖 | 允许 |
|---|---|
| `get_current_user` | 任何已登录用户 |
| `require_roles("admin")` | 管理员 |
| `require_roles("admin", "developer")` | 管理员与开发者 |

各接口的角色要求见第 4 节。

### 3.3 API Key 调用（FR-021）

- 请求头复用 `Authorization: Bearer ak_xxx`。`get_current_user` 按 `ak_` 前缀分流到 `api_key_service.authenticate`，判定顺序固定：① 查 `api_keys.key_hash = sha256(明文)` 且 `is_enabled`；② 归属用户存在且启用；③ 来源 IP 在 `allowed_ips` 白名单内（空名单不限制）；④ 限流：单 Key `rate_limit_per_minute`，为 0 时用全局 `RATE_LIMIT_API_KEY_PER_MINUTE`，按自然分钟固定窗口计数；⑤ 用带前置条件的 `UPDATE ... WHERE used < quota` 扣 1 次配额并更新 `last_used_at`。
- 通过后以 Key 归属用户的身份与角色执行，等同于该用户登录。**每个通过鉴权的请求消耗 1 次配额**，与业务是否成功无关；被白名单拒绝或被限流的请求不扣配额。
- 错误：401 "API Key 无效或已停用"；403 "API Key 归属账号不可用"；403 "API Key 不允许从该 IP 调用"（同时写审计 `api_key_ip_rejected`，detail 含来源 IP 与 Key 前缀）；429 "请求过于频繁，请 N 秒后重试"（限流）；429 "API Key 配额已用尽"（不再累加 used）。
- 限流依赖 Redis：Redis 不可用时放行并记录故障，`/system/status.rate_limit.enabled=false` 且进 `degraded`；`RATE_LIMIT_ENABLED=false` 是配置关闭，不算降级。限流的 429 带 `Retry-After` 与 `X-RateLimit-*`，通过的请求带剩余额度，见 2.2。
- 可用范围：所有只要求登录的接口（`/auth/me`、`/agents/{id}/chat`、`/conversations*`），以及显式开放的 `POST /workflows/{id}/run` 与 `POST /workflows/{id}/runs/{run_id}/resume`。其余管理接口对 API Key 返回 403 "API Key 不能访问管理接口"。
- 开放新的执行类接口给 API Key：路由依赖改为 `require_roles(..., allow_api_key=True)`，并在本节登记。

## 4. 接口清单

角色列：`*` 任何登录用户，`A` 管理员，`AD` 管理员与开发者。

### 4.1 认证 `/auth`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| POST /auth/login | 公开 | `{username, password}` | `{token, user: {id, username, role, is_active}}`；401 密码错、403 停用、429 锁定或来源 IP 每分钟超过 `RATE_LIMIT_IP_PER_MINUTE` |
| GET /auth/me | * | | `{id, username, role, is_active}` |

### 4.2 用户 `/users`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /users | A | 分页参数；`q` 用户名模糊；`role`；`is_active`；sort = id / username / created_at | 分页信封，items `[{id, username, role, is_active, created_at, updated_at}]` |
| POST /users | A | `{username, password, role="caller"}` | 用户对象；409 重名 |
| POST /users/batch | A | `{ids, action: enable / disable / delete}` | 批量信封；对自己的停用 / 删除 400 进失败清单 |
| PUT /users/{id} | A | `{role?, is_active?}` | 用户对象；停用或降级当前登录的管理员 → 400 |
| POST /users/{id}/reset-password | A | `{password}`（6～128 字符） | `{code:0}`；写审计 `reset_password`（不记密码） |
| DELETE /users/{id} | A | | `{code:0}`；409 仍持有模型/智能体/知识库/工作流；删除自己 → 400 |

### 4.3 模型 `/models`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /models | AD | 分页参数；`q` 名称模糊；`provider`；`is_enabled`；sort = id / name / provider / updated_at | 分页信封，items `[{id, name, provider, api_base, model_name, default_params, is_enabled, price_input, price_output, agents_count, created_by, created_by_username, created_at, updated_at}]`，不含密钥 |
| POST /models | A | `{name, provider, api_base, api_key, model_name, default_params={}, price_input?, price_output?}`；`default_params.thinking` 只接受 `disabled / enabled`（422），见 `03` 4.1 | 模型对象 |
| POST /models/batch | A | `{ids, action: enable / disable / delete}` | 批量信封；被引用的模型删除 409 进失败清单 |
| GET /models/{id} | AD | | 模型对象 + `agents: [{id, name, status}]`（引用它的智能体） |
| PUT /models/{id} | A | 同 POST；`api_key` 留空表示沿用已有密钥 | 模型对象 |
| POST /models/{id}/toggle | A | | `{id, is_enabled}`；写审计 `enable / disable`；停用后引用它的智能体对话 400 "模型不可用" |
| DELETE /models/{id} | A | | `{code:0}`；409 被智能体引用 |
| POST /models/{id}/test | AD | | `{code:0, message, data: {ok: true, reply}}` 或 `{ok: false, error}`；以探测模式绕过熔断的打开期判定，成功即关闭该模型的熔断（人工恢复手段） |

### 4.4 智能体 `/agents`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /agents | AD | 分页参数；`q` 名称模糊；`status` = draft / published；`model_id`；`kb_id`（绑定了该知识库）；`tool_id`；`prompt_template_id`；sort = id / name / status / version / updated_at | 分页信封，items `[{id, name, description, system_prompt, model_id, params, kb_ids, tool_ids, workflow_id, status, version, prompt_template_id, prompt_template_version, prompt_variables, prompt_template_outdated, model_name, prompt_template_name, created_by, created_by_username, created_at, updated_at, runs_7d, last_run_at}]`；关联字段都是一页一次 IN 查询批量装配 |
| POST /agents/batch | AD | `{ids, action: publish / delete}` | 批量信封 |
| POST /agents | AD | `{name, description="", system_prompt="", model_id, params={}, kb_ids=[], tool_ids=[], workflow_id?, prompt_template_id?, prompt_variables={}}`；传 `prompt_template_id` 时 `system_prompt` 必须省略或为空（否则 400 "绑定模板时不能同时手填 system_prompt"），服务端用模板当前版本 + `prompt_variables` 渲染写入 `system_prompt`，缺必填变量 400 "缺少必填变量：x" 且不落库；模板不存在 404 | 智能体对象，status=draft |
| GET /agents/{id} | AD | | 智能体对象 + 关联对象：`model {id, name, provider, model_name, is_enabled}`、`tools [{id, name, type, is_enabled}]`、`knowledge_bases [{id, name, is_public}]`、`workflow {id, name, status}`、`prompt_template {id, name, version, variables}`，以及悬空引用 `missing_tool_ids / missing_kb_ids`（知识库或工具删除后残留在 JSONB 里的 ID） |
| PUT /agents/{id} | AD | 同 POST，整体覆盖 | 智能体对象；不改变 status。绑定模板的智能体重新保存即按模板当前版本重新渲染，`prompt_template_outdated` 随之为 false；对话立即生效（读的是 agents 行） |
| DELETE /agents/{id} | AD | | `{code:0}`；会话、消息、运行记录级联删除 |
| POST /agents/{id}/publish | AD | | 智能体对象，status=published，version+1，写快照（含三个模板字段）与审计 |
| GET /agents/{id}/versions | AD | 分页参数 | 分页信封，items `[{id, version, snapshot, created_at}]` 版本倒序 |
| POST /agents/{id}/rollback/{version_id} | AD | | 智能体对象，配置恢复到快照（含模板三字段；回滚到绑定模板之前的版本则三字段为空），version+1；404 版本不属于该智能体 |

### 4.5 对话 `/agents/{id}/chat`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| POST /agents/{id}/chat | * | `{message, conversation_id?}` | `text/event-stream`，事件见第 5 节；403 未发布；404 会话不属于本人 |

`conversation_id` 为空时新建会话，标题取消息前 `CHAT_TITLE_MAX_LEN` 字。

### 4.6 会话 `/conversations`（只能操作本人的）

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /conversations | * | 分页参数；`agent_id`；`q` 标题模糊 | 分页信封，items `[{id, agent_id, agent_name, title, summary, message_count, created_at, updated_at}]` 按更新时间倒序。`summary` 可空，是更早消息的持久化摘要（≤ 150 字），仅用于排查，API Key 调用方同样可见 |
| GET /conversations/{id}/messages | * | | `[{id, role, content, tool_calls, citations, token_usage, created_at}]`；`token_usage` 只有 assistant 消息有，其余为 `null` |
| DELETE /conversations/{id} | * | | `{code:0}` |

### 4.7 工具 `/tools`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /tools | AD | 分页参数；`q` 名称模糊；`type` = builtin / http；`is_enabled`；sort = id / name / type / timeout | 分页信封，items `[{id, name, description, type, config, timeout, is_enabled, agents_count}]` |
| POST /tools/batch | AD | `{ids, action: enable / disable / delete}` | 批量信封 |
| GET /tools/{id} | AD | | 工具对象 + `agents: [{id, name, status}]` |
| POST /tools/{id}/toggle | AD | | `{id, is_enabled}`；停用的工具不再暴露给模型，工作流 `tool` 节点调用它时节点 failed "工具已停用" |
| POST /tools | AD | `{name, description, type="builtin", config={}, timeout=30}`；HTTP 工具的 `config.parameters` 按 `03` 4.2 的子集校验，不合法 422（`loc=["body","config"]`，msg 以"config.parameters 不合法："开头并指出位置） | 工具对象（`config.parameters` 已规范化） |
| PUT /tools/{id} | AD | 同 POST | 工具对象 |
| DELETE /tools/{id} | AD | | `{code:0}` |
| POST /tools/{id}/test | AD | `{args={}}`；HTTP 工具先按声明校验 `args`（宽松转换：`"3"` → 3；未声明的键、缺必填、枚举外、类型不符都拒绝），不合法 400 "参数校验失败：<位置: 原因>" 且不发起调用 | `{code:0, message, data: {result: <执行结果>}}`；执行失败时 result 为 `{error}` |

智能体对话中 HTTP 工具按声明以结构化参数暴露给模型（`StructuredTool.args_schema`）；模型传参不符合声明时不中断对话，工具结果返回"参数校验失败：..."让模型纠正。工作流 `tool` 节点的固定 `args` 同样在运行前校验，不合法时节点 failed、运行 failed，错误文本相同。

典型 422（`type: array`）：

```json
{"detail": [{"type": "value_error", "loc": ["body", "config"],
             "msg": "Value error, config.parameters 不合法：properties.tags.type: Input should be 'string', 'number', 'integer' or 'boolean'", ...}]}
```

典型 400（测试调用缺必填）：`{"detail": "参数校验失败：city: Field required", "trace_id": "..."}`

### 4.8 知识库 `/knowledge-bases`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /knowledge-bases | AD | 分页参数；`q` 名称模糊；`is_public`；sort = id / name / updated_at | 分页信封，items `[{id, name, description, embedding_model, chunk_size, chunk_overlap, is_public, visible_roles, policy_version, document_count, ready_count, failed_count, processing_count, chunk_count, token_count, created_by, created_by_username, created_at, updated_at}]` |
| POST /knowledge-bases | AD | `{name, description="", embedding_model="text-embedding-3-small", chunk_size=500, chunk_overlap=50, is_public=true, visible_roles=[]}` | 知识库对象 |
| POST /knowledge-bases/batch | AD | `{ids, action: delete}` | 批量信封 |
| GET /knowledge-bases/{id} | AD | | 知识库对象 + `agents: [{id, name, status}]`（绑定它的智能体） |
| PUT /knowledge-bases/{id} | AD | 同 POST；更新 name、description、切片参数（只影响之后上传的文档）、权限；不改 embedding_model | 知识库对象 |
| DELETE /knowledge-bases/{id} | AD | | `{code:0}`；文档与切片级联删除 |
| POST /knowledge-bases/{id}/documents | AD | multipart `file` | `{id, kb_id, name, file_type, status: "uploading"}`，处理在后台 |
| GET /knowledge-bases/{id}/documents | AD | 分页参数；`status` = uploading / parsing / chunking / ready / failed；`q` 文件名模糊；`file_type`；`created_from / created_to`；sort = id / name / status / chunk_count, chunk_total, processing_started_at, finished_at / created_at | 分页信封，items `[{id, kb_id, name, file_type, status, chunk_count, error, created_at}]` 默认 id 倒序 |。`chunk_total`（计划切片数）与 `processing_started_at` 在分片完成时写入，`chunk_count` 每 100 片递增一次，`finished_at` 在 ready / failed 时写入；前端据此显示进度、速度与预计剩余（2026-09-06）
| POST /knowledge-bases/{id}/documents/batch | AD | `{ids, action: delete / reprocess}` | 批量信封；reprocess 的文档排进后台任务 |
| POST /knowledge-bases/{id}/documents/{doc_id}/reprocess | AD | | `{id, status: "uploading"}`：清掉旧切片与错误后重新走解析管道；处理中的文档 400 |
| GET /knowledge-bases/{id}/documents/{doc_id}/chunks | AD | 分页参数 | 分页信封 + `doc_id, doc_name`；items `[{id, content, meta, token_count}]`，`total` 即切片总数 |
| DELETE /knowledge-bases/{id}/documents/{doc_id} | AD | | `{code:0}`；MinIO 对象不删除 |
| POST /knowledge-bases/{id}/search | AD | `{query, top_k=4, debug=false}` | 见下；item 带 `rerank_mode`（model / lexical）与 `rerank_score`（模型分，词法为 null），`stats.rerank_mode` 同 |

检索响应：

```json
{
  "items": [{"content", "score", "chunk_id", "doc_id", "doc_name", "meta",
             "vector_score", "keyword_score", "matched_keywords"}],
  "stats": {"query", "keywords", "candidate_count", "acl_rejected", "returned", "rerank_mode", "timings": {"embed_ms", "vector_ms", "keyword_ms", "keyword_count", "rerank_ms"},
            "top_score", "mean_score", "lexical_hit_count"}
}
```
`debug=false` 时只有 `items`，且不含三个评分细分字段。检索按调用者角色做权限过滤。

### 4.9 工作流 `/workflows`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /workflows | AD | 分页参数；`q` 名称模糊；`status`；sort = id / name / status / version / updated_at | 分页信封，items `[{id, name, description, status, version, node_count, node_types, schedules_count, created_by, created_by_username, created_at, updated_at, runs_7d, last_run_at}]` |
| POST /workflows | AD | `{name, description="", graph}` | 同上单个；400 图校验失败 |
| POST /workflows/test-run | AD | `{graph, input=""}` | `{status, output?, interrupt?, error?, steps}`，不落库；400 图校验失败 |
| POST /workflows/batch | AD | `{ids, action: delete}` | 批量信封；被智能体引用的 409 进失败清单 |
| GET /workflows/{id} | AD | | 列表字段 + `graph` + `agents: [{id, name, status}]` + `schedules: [{id, name, cron, is_enabled, last_run_at}]` |
| POST /workflows/{id}/duplicate | AD | | 新工作流对象（草稿、版本 1、名称加"副本"） |
| PUT /workflows/{id} | AD | `{name, description, graph}` | `{id, name, version}`，version+1；400 图校验失败 |
| DELETE /workflows/{id} | AD | | `{code:0}`；409 被智能体引用 |
| POST /workflows/{id}/run | AD | `{input=""}` | `{run_id, status, output?, interrupt?, error?, steps}`；400 图校验失败（在创建运行记录之前，不产生记录） |
| POST /workflows/{id}/runs/{run_id}/resume | AD | `{decision: {...}}` | 同上；400 运行不在 awaiting_review |
| GET /workflows/{id}/runs | AD | 分页参数；`status`；`started_from / started_to`；sort 同 `/runs` | 分页信封，items 与 `GET /runs` 相同（委托同一实现） |

`status` 取 `success / awaiting_review / failed`。`decision` 会原样传给人工审核节点，前端约定 `{"decision": "approved"}` 或 `{"decision": "rejected"}`。

图校验（FR-029，规则见 `11` 4.5.3）失败的 detail 是逐条错误用分号连接：`{"detail": "图校验失败：并行节点 par 至少需要 2 条分支；汇聚节点 join 的入边必须全部来自同一个并行节点且不少于 2 条", "trace_id": "..."}`。不含 `parallel / join` 的图不受影响。含并行的运行结果里 `steps` 中两条分支的条目顺序不保证；`output` 在 `join` 之后是 `{分支末节点 id: 输出}` 字典。

### 4.10 运行记录 `/runs`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /runs | AD | 分页参数；`run_type` = chat / workflow；`status`；`agent_id`；`workflow_id`；`user_id`；`model_id`；`source` = chat / ui / api_key / schedule；`started_from / started_to`；sort = id / started_at / finished_at / latency_ms / cost | 分页信封，items `[{id, run_type, agent_id, agent_name, workflow_id, workflow_name, user_id, username, model_id, model_name, conversation_id, source, schedule_id, status, error, output, latency_ms, token_usage, cost, started_at, finished_at}]` |
| GET /runs/summary | AD | 与列表相同的过滤参数（不含 status） | `{total, running, success, failed, cancelled, awaiting_review, prompt_tokens, completion_tokens, total_tokens, total_cost, avg_latency_ms, p50_latency_ms, p95_latency_ms, success_rate, latency_buckets: [{label, from_ms, to_ms, count}]}`，随筛选联动 |
| GET /runs/{id} | AD | | 上述字段 + `input` + `nodes: [{id, node_id, node_type, status, error, input, output, started_at, finished_at, duration_ms}]` |

- `cost` 是**收尾时的快照**：按当时智能体绑定模型的单价折算后落库，之后改单价不追溯；无用量、无模型（工作流运行）或未配单价时为 `null`，汇总求和按 0。`model_id` 同样是发起时的快照，智能体换模型后旧运行仍归旧模型。
- `source` 记触发来源：对话 `chat`、界面运行工作流 `ui`、API Key 运行工作流 `api_key`、定时任务 `schedule`（另带 `schedule_id`）。
- 耗时相关指标（平均、P50、P95、分布桶）只统计已结束的运行；`success_rate = success / (success + failed)`，分母为 0 时 `null`。分布桶固定六档：`<1s`、`1-3s`、`3-10s`、`10-30s`、`30-60s`、`>60s`。
- 节点日志的 `input / output` 是引擎截断到 500 字符的文本快照，`duration_ms` 由起止时间算。
- `status` 取 `running / success / failed / cancelled / awaiting_review`。

### 4.11 审计日志 `/audit-logs`

| 方法 路径 | 角色 | 响应 |
|---|---|---|
| GET /audit-logs | A | 分页参数；`action`；`resource`；`username` 模糊；`resource_id`；`ip`；`created_from / created_to`；sort = id / created_at → 分页信封，items `[{id, user_id, username, action, resource, resource_id, detail, ip, created_at}]` |

`action` 取值：`login / login_failed / create / delete / publish / rollback / enable / disable / reset_password / rag_retrieve / api_key_ip_rejected`；`resource` 取值：`auth / model / agent / prompt_template / user / api_key / knowledge_base`。

### 4.12 API Key `/api-keys`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /api-keys | AD | 分页参数；`q` 名称模糊；`is_enabled`；`user_id`（按创建人，仅 admin 生效）；sort = id / name / used / last_used_at / created_at | 分页信封，items `[{id, name, key_prefix, quota, used, is_enabled, allowed_ips, rate_limit_per_minute, user_id, username, last_used_at, created_at}]`；developer 只返回本人创建的 |
| POST /api-keys | AD | `{name, quota=1000, allowed_ips=[], rate_limit_per_minute=0}`；`allowed_ips` 每项须为合法 IP/CIDR 且 ≤ 50 项、限速 0～10000，违反 422 | 列表对象的全部字段 + `key`，**`key` 明文只在此处返回一次** |
| POST /api-keys/batch | AD | `{ids, action: enable / disable / delete}` | 批量信封；developer 对他人 Key 的 404 进失败清单 |
| PUT /api-keys/{id} | AD | `{name?, quota?, allowed_ips?, rate_limit_per_minute?}`，只更新传了的字段 | 列表对象；developer 改他人创建的 → 404 |
| POST /api-keys/{id}/toggle | AD | | `{id, is_enabled}`；developer 操作他人创建的 → 404 |
| DELETE /api-keys/{id} | AD | | `{code:0}`；developer 删他人创建的 → 404 |

### 4.13 定时任务 `/schedules`

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /schedules | AD | 分页参数；`q` 名称模糊；`workflow_id`；`is_enabled`；sort = id / name / last_run_at / created_at | 分页信封，items `[{id, name, workflow_id, workflow_name, cron, cron_valid, input, is_enabled, user_id, username, last_run_at, last_run_id, last_run_status, next_run_at, created_at}]` |
| POST /schedules | AD | `{name, workflow_id, cron, input={}}`；**cron 非法 422**（五段 crontab） | 任务对象；工作流不存在 404 |
| POST /schedules/batch | AD | `{ids, action: enable / disable / delete}` | 批量信封 |
| GET /schedules/{id} | AD | | 任务对象 |
| PUT /schedules/{id} | AD | 同 POST，整体覆盖 | 任务对象；启用中的任务按新配置重新注册 |
| POST /schedules/{id}/toggle | AD | | `{id, is_enabled}` |
| POST /schedules/{id}/run | AD | | `{id, triggered_at}`：立即执行一次（异步，停用的任务也可手动跑），结果去运行记录看（`source=schedule`）；cron 非法 400 |
| DELETE /schedules/{id} | AD | | `{code:0}` |

`input` 形如 `{"input": "传给工作流的字符串"}`。`next_run_at` 来自本进程的调度器，任务停用或未注册时为 `null`；`cron_valid=false` 是存量非法表达式（创建 / 编辑已拦 422）。`last_run_id / last_run_status` 取运行记录里 `input.schedule_id` 指向该任务的最近一条。

### 4.14 系统状态 `/system`

| 方法 路径 | 角色 | 响应 |
|---|---|---|
| GET /system/status | AD | 见下；含依赖可用性，不对 caller 与 API Key 开放。2026-09-06 起多 `rerank` 块 `{mode, configured, provider, model, reason, last_error}`，配置了但调用失败时 `degraded` 含 `rerank` |

```json
{
  "app": "Agent Platform",
  "database": {"ok": true, "reason": null},
  "embedding": {"mode": "hash", "model": "hash-fallback", "dim": 1024,
                "configured": false, "reason": "未配置 EMBEDDING_API_KEY…", "last_error": null},
  "login_guard": {"enabled": true, "reason": null, "max_fail": 5, "lock_seconds": 600},
  "rate_limit": {"enabled": true, "configured": true, "reason": null,
                 "api_key_per_minute": 60, "user_per_minute": 300, "ip_per_minute": 20},
  "model_breakers": [{"model_id": 3, "name": "deepseek", "state": "open", "consecutive_failures": 5,
                      "opened_at": "2026-09-05T04:00:00+00:00", "retry_after_seconds": 12}],
  "scheduler": {"running": true, "registered_jobs": 0, "enabled_jobs": 0},
  "degraded": [{"item": "embedding", "message": "未配置 EMBEDDING_API_KEY…"}]
}
```

- `embedding.mode`：`model` 用真实向量模型，`hash` 表示检索正在降级运行。
- `embedding.configured=false` 是配置性降级（没配 key），`configured=true` 且 `last_error` 非空是故障性降级。
- `login_guard.enabled=false` 表示 Redis 不可用、登录限流当前没有生效。
- `rate_limit.configured=false` 是配置关闭（`RATE_LIMIT_ENABLED=false`，不进 `degraded`）；`configured=true` 且 `enabled=false` 是 Redis 故障，进 `degraded`。
- `model_breakers` 只列非 closed 的模型：`open` 表示打开期内直接 503，`retry_after_seconds` 后自动放行一个探测；`half_open` 表示探测进行中。`open` 的模型进 `degraded`（`item=model_breaker`）。状态在进程内，多实例各自独立。
- `scheduler.registered_jobs` 小于 `enabled_jobs` 通常意味着有任务的 cron 表达式非法被跳过。
- `degraded` 为空即一切正常；前端只看这一项就能决定是否提示。

### 4.15 健康检查

`GET /health` → `{"status": "ok", "app": "<APP_NAME>", "embedding_mode": "model|hash"}`。

不查库、不连 Redis，只报进程存活与向量后端模式，供探活与快速判断是否降级。完整依赖状态用 `/system/status`。

### 4.16 提示词模板 `/prompt-templates`（AD；API Key 403）

| 方法 路径 | 角色 | 请求 | 响应 |
|---|---|---|---|
| GET /prompt-templates | AD | 分页参数；`q` 名称模糊；sort = id / name / version / updated_at | 分页信封，items `[{id, name, description, variables, version, created_by, created_by_username, updated_at, agents_count}]`（不含 content） |
| POST /prompt-templates/batch | AD | `{ids, action: delete}` | 批量信封；仍被绑定的 409 进失败清单 |
| GET /prompt-templates/{id}/agents | AD | | `[{id, name, status, prompt_template_version, outdated}]` 绑定该模板的智能体 |
| POST /prompt-templates | AD | `{name, description="", content, variables=[]}`；`variables` 每项 `{name, description="", required=false, default=null}`，≤ 30 个、同名唯一（违反 422） | 模板对象 `{…, content, created_at, unused_variables}`；409 重名；400 "模板引用了未声明的变量：x, y" |
| GET /prompt-templates/{id} | AD | | 模板对象 |
| PUT /prompt-templates/{id} | AD | 同 POST，整体覆盖 | 模板对象；content / variables 变化时 version+1 并写快照，只改名称 / 描述不升版本 |
| DELETE /prompt-templates/{id} | AD | | `{code:0}`；版本快照级联删除；仍被智能体绑定 → 409 "仍有 N 个智能体绑定该模板"（解绑后可删） |
| GET /prompt-templates/{id}/versions | AD | 分页参数 | 分页信封，items `[{id, version, content, variables, created_at}]` 版本倒序（初版也有快照） |
| POST /prompt-templates/{id}/rollback/{version_id} | AD | | 模板对象，内容与变量恢复到该快照且 version+1；404 版本不属于该模板 |
| POST /prompt-templates/{id}/render | AD | `{variables={}}` | `{content, missing: [], unused: []}`；400 "缺少必填变量：x" |

渲染是纯字符串替换：占位符 `{{name}}`（允许两侧空格），传了值用值、否则用 `default`，空串视为未传；未声明的键忽略。审计动作：`create / delete / rollback`，资源 `prompt_template`。

示例：

```
POST /prompt-templates
{"name": "客服", "content": "你是{{role}}，请用{{tone}}的语气回答。",
 "variables": [{"name": "role", "required": true}, {"name": "tone", "default": "友好"}, {"name": "spare"}]}
→ 200 {"id": 1, "name": "客服", "version": 1, "content": "...", "variables": [...], "unused_variables": ["spare"], ...}

POST /prompt-templates/1/render  {"variables": {"role": "客服"}}
→ 200 {"content": "你是客服，请用友好的语气回答。", "missing": [], "unused": ["spare"]}
POST /prompt-templates/1/render  {"variables": {}}
→ 400 {"detail": "缺少必填变量：role", "trace_id": "..."}
```

### 4.17 运营统计 `/stats`（AD；API Key 403）

全部只读，数据库侧聚合；按天分桶按 `REPORT_TIMEZONE`（默认 Asia/Shanghai）切天，`days` 含今天、上限 `STATS_MAX_DAYS`（默认 90，超出按上限截断，0 → 422）。

| 方法 路径 | 请求 | 响应 |
|---|---|---|
| GET /stats/overview | | `{resources: {agents, published_agents, models, enabled_models, workflows, knowledge_bases, documents, tools, prompt_templates, users, api_keys, schedules}, today: 指标, last_7d: 指标, pending: {awaiting_review, running, stuck_running, failed_today, failed_documents, processing_documents, open_breakers, unregistered_schedules}, degraded: 同 /system/status, scheduler: 同 /system/status, recent_runs: 最近 8 条运行（字段同 GET /runs）}` |
| GET /stats/runs/daily | `days=30`；`run_type`；`agent_id`；`workflow_id` | `{days, timezone, items: [{date: "YYYY-MM-DD", …指标}]}`，缺失日期补零、按日期升序 |
| GET /stats/runs/summary | 同上 | `{days, …指标}` |
| GET /stats/models | `days`；`model_id` | `{days, items: [{model_id, name, provider, model_name, is_enabled, agents_count, breaker, …指标}]}`，含用量为 0 的模型，按运行数倒序 |
| GET /stats/agents | `days`；`agent_id` | `{days, items: [{agent_id, name, status, model_id, model_name, conversations, messages, last_run_at, …指标}]}` |
| GET /stats/workflows | `days`；`workflow_id` | `{days, items: [{workflow_id, name, status, last_run_at, …指标}]}` |

"指标"固定为 `{total, success, failed, cancelled, awaiting_review, running, prompt_tokens, completion_tokens, total_tokens, cost, avg_latency_ms, success_rate}`；`avg_latency_ms` 只算已结束的运行，`success_rate` 分母为 0 时 `null`，`cost` 读运行记录的快照列。`stuck_running` 是超过 1 小时仍 running 的记录（进程被杀后没收尾的幽灵记录）。

## 5. 对话 SSE 事件契约

响应头 `Content-Type: text/event-stream`，每个事件一行 `data: <JSON>` 加空行。事件顺序：`citations` → 若干 `delta` / `tool_call` / `tool_result` → `done`；任何阶段可能以 `error` 结束。

| type | 字段 | 说明 |
|---|---|---|
| citations | `citations: [{kb_id, chunk_id, doc_name, content, score}]` | 首个事件；无知识库时为空数组 |
| delta | `content` | 回复文本增量 |
| tool_call | `id, name, arguments` | 模型决定调用工具；`arguments` 为解析后的对象，解析失败为 `{"_raw": 字符串}` |
| tool_result | `tool_call_id, content` | 工具返回，截断 200 字符 |
| done | `message_id, run_id, conversation_id, usage: {prompt_tokens, completion_tokens, total_tokens}` | 正常结束 |
| error | `message` | 出错结束；此时运行记录已置 failed |

客户端主动断开（停止按钮）不会收到 `done`；服务端把已生成内容落库，运行记录置 `cancelled`。

前端解析器在 `frontend/src/api/index.ts` 的 `chatAgentStream`，新增事件类型时两端同批改。

## 6. 契约演进规则

- 改字段名、类型、枚举值：一次改到底，同批改路由、服务、前端 api 层与页面、本文档；不留 `新字段 || 旧字段` 兼容。
- 新增可选字段可以只加不删；删除字段先全仓搜索引用为零。
- 有第二个消费者（API Key 调用方）之后，破坏性变更要在本文档记录废弃时间。
