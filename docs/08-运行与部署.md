# 智枢·智能体平台 运行与部署

版本 V2.3 · 2026-09-06

> 本地开发、环境变量、数据库初始化与迁移、两种部署形态、日志与排障。改了启动方式或配置项必须同批更新本文档。

## 1. 环境要求

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | 3.12+ | LangChain 1.x 需 3.10+；用 uv 管理虚拟环境与依赖 |
| Node.js | 20+ | 前端构建 |
| PostgreSQL | 16 + pgvector 扩展 | 推荐镜像 `pgvector/pgvector:pg16` |
| Redis | 7 | 只用于登录限流；不可用时限流静默关闭 |
| MinIO | 任意近期版本 | 知识库文档原文存储，桶不存在会自动创建 |
| tesseract | 可选 | 图片 OCR；未安装时图片文档解析为空文本 |

## 2. 本地开发

### 2.1 后端

```bash
cd backend
uv venv .venv --python 3.12
uv pip install --python .venv/bin/python -r requirements.txt pytest
cp .env.example .env            # 按第 3 节修改
PGPASSWORD=<密码> PGUSER=admin .venv/bin/python scripts/setup_db.py   # 建库 + vector 扩展，只需一次
.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
curl http://127.0.0.1:8000/health
```

- 应用启动时会自动建缺失的表并创建 `admin / admin123`，`scripts/init_db.py` 只是同样逻辑的手动版本。
- `setup_db.py` 读 `PGHOST / PGPORT / PGUSER / PGPASSWORD / PGDATABASE` 环境变量，默认用户 `admin`、库 `agent_platform`。
- 验证导入：`.venv/bin/python -c "from app.main import app"`。

### 2.2 前端

```bash
cd frontend
npm install
npm run dev -- --port 18056 --host 0.0.0.0
```

Vite 把 `^/api/` 代理到 `http://127.0.0.1:8000`（`vite.config.ts`），所以后端要先起来。代理键必须是带尾斜杠的正则：写成 `/api` 会按前缀把前端路由 `/api-keys` 也转给后端，刷新该页看到的是后端 404（2026-09-06 修正；`vite preview` 沿用同一配置）。类型检查 `npx tsc --noEmit`，构建 `npm run build`。

**改过 `vite.config.ts` 的构建配置（分块、压缩、环境变量注入）后，必须 `npm run build && npx vite preview --port 4173` 用生产包实际打开验证**：开发服务器不分块、不压缩，2026-09-06 分块成环导致的整站白屏在本地 `npm run dev` 下完全看不出来，部署后才暴露。

前端构建时变量放 `frontend/.env.local`（模板见 `frontend/.env.example`），只有 `VITE_` 前缀会打进产物，不要放密钥：

| 变量 | 默认 | 说明 |
|---|---|---|
| VITE_LOGIN_HINT | 空 | 登录页页脚提示（演示环境可写默认账号）；不设置则不显示。默认账号不再写死在代码里 |

### 2.3 连接共享数据服务时

`.env` 里的 `DATABASE_URL / REDIS_URL / MINIO_ENDPOINT` 可以指向远程服务，这样本地不用装数据库。此时要记住：

- 库里是别人也在用的数据，删表、清数据、批量更新前先确认。
- 每个连上这个库的后端进程都会加载并触发启用中的定时任务，本地跑后端或 pytest 时也会触发。
- `EMBEDDING_DIM` 必须与库里向量列的维度一致（查法见第 8 节）。

## 3. 环境变量

后端读 `backend/.env`（pydantic-settings，`extra=ignore`）；Docker Compose 读根目录 `.env` 再注入容器。

| 变量 | 默认 | 说明 |
|---|---|---|
| APP_NAME | Agent Platform | 健康检查与文档标题 |
| API_V1_PREFIX | /api/v1 | 改了前端代理与 nginx 要同步 |
| SECRET_KEY | change-me | JWT 签名密钥，部署必改 |
| ACCESS_TOKEN_EXPIRE_MINUTES | 1440 | token 有效期 |
| AES_KEY | change-me-32-bytes-key | 派生 Fernet 密钥加密模型 API Key。**入库后不能再改** |
| DATABASE_URL | postgresql+psycopg2://postgres:postgres@localhost:5432/agent_platform | |
| REDIS_URL | redis://localhost:6379/0 | 带密码写 `redis://:密码@host:6379/0` |
| MINIO_ENDPOINT | localhost:9000 | 不带协议，当前固定 HTTP |
| MINIO_ACCESS_KEY / MINIO_SECRET_KEY | minioadmin | |
| MINIO_BUCKET | kb-docs | |
| MODEL_HTTP_TIMEOUT | 120 | 模型调用超时秒数（流式长回复需要，保持不变） |
| MODEL_MAX_RETRIES | 0 | OpenAI SDK 自身的重试次数；不设时 SDK 默认 2 次，超时类故障一次"失败"要等 3 × 超时。故障处理交给熔断器 |
| MODEL_BREAKER_FAIL_THRESHOLD | 5 | 模型连续失败（连接 / 超时 / 429 / 5xx）多少次打开熔断；0 关闭熔断 |
| MODEL_BREAKER_OPEN_SECONDS | 30 | 熔断打开后多少秒内直接 503 不请求上游，到期放行一个探测。状态在进程内，多实例各自熔断，连通测试只恢复当前实例 |
| RAG_TOP_K | 4 | 每个知识库每个子查询召回条数 |
| RAG_QUERY_REWRITE_ENABLED | false | 检索前是否让模型把问题改写成 3 个子查询。开启后每条消息多一次模型调用（DeepSeek 类模型实测 4～9 秒才返回），2026-09-06 起默认关闭，只在召回质量明显不足时打开 |
| RAG_QUERY_REWRITE_TIMEOUT_SECONDS | 3 | 改写调用的本地超时；超时退回原问题检索，并计入该模型的连续失败 |
| CHAT_HISTORY_MAX_MESSAGES | 20 | 多轮保留的原文消息条数，更早的压缩为摘要并持久化到会话 |
| CHAT_SUMMARY_BATCH_MESSAGES | 10 | 更早消息里未折叠进摘要的攒够多少条才调一次模型更新会话摘要；不足一批按原文注入 |
| REPORT_TIMEZONE | Asia/Shanghai | 运营统计按天分桶用的时区（库里存 UTC）；改成业务所在时区，否则"今天"的口径会偏 |
| STATS_MAX_DAYS | 90 | 统计接口 `days` 参数的上限，超出按上限截断 |
| CHAT_TITLE_MAX_LEN | 30 | 会话标题截断长度 |
| TOOL_CALL_MAX_ROUNDS | 8 | 预留，当前未使用 |
| LOOP_MAX_ITERATIONS | 20 | 预留，当前未使用 |
| EMBEDDING_API_BASE | 空 | OpenAI 兼容 embedding 接口地址；空则 hash 向量 |
| EMBEDDING_API_KEY | 空 | 空则不调接口，直接 hash 向量 |
| EMBEDDING_MODEL | text-embedding-3-small | |
| EMBEDDING_DIM | 1536 | 必须等于 `document_chunks.embedding` 列维度 |
| CORS_ORIGINS | http://localhost:18056 | 浏览器直连后端时允许的源，逗号分隔，每项形如 `http://host:port`（无路径、无末尾斜杠）；经 vite preview / nginx 的 `/api` 同源反代时不涉及。非法值启动报错 |
| IP_DENYLIST | 空 | 全局 IP 黑名单，逗号分隔的 IP 或 CIDR，命中一律 403（`/health` 除外）。非法值启动报错 |
| TRUSTED_PROXY_ENABLED | false | 是否信任反代传来的 `X-Real-IP` / `X-Forwarded-For`。只有后端只能经反代到达时才开：能直连后端的调用方可以伪造这两个头，见第 7 节"来源 IP 与反代" |
| RATE_LIMIT_ENABLED | true | 入口限流总开关；关闭时不计数、不进 `degraded` |
| RATE_LIMIT_API_KEY_PER_MINUTE | 60 | 每个 API Key 每分钟请求上限（自然分钟固定窗口）；单 Key 的 `rate_limit_per_minute` 非 0 时覆盖 |
| RATE_LIMIT_USER_PER_MINUTE | 300 | 每个登录用户（JWT）每分钟上限 |
| RATE_LIMIT_IP_PER_MINUTE | 20 | 登录接口按来源 IP 每分钟上限 |

新增配置项的五处同步要求见 `06-后端规范.md` 第 9 节。

## 4. 数据库初始化与迁移

1. 首次：`setup_db.py` 建库与扩展 → 启动应用自动建表。
2. 之后每次拉取代码：查看 `backend/scripts/migrations/` 里有没有新脚本，按文件名顺序执行未跑过的：

```bash
cd backend
.venv/bin/python scripts/migrations/<文件名>.py          # 预览
.venv/bin/python scripts/migrations/<文件名>.py --apply  # 有破坏性操作的脚本需要显式 --apply
```

脚本幂等，重复执行无副作用。目前没有记录"已执行到哪个脚本"的表，靠人工核对；引入 Alembic 在演进路线里。

## 5. 测试

```bash
cd backend
PYTHONPATH=. .venv/bin/python -m pytest tests/ -v
```

- 测试连接 `.env` 指向的库，用 `admin / admin123` 登录，数据以 `pytest-` 前缀命名并自行清理。
- CI（`.github/workflows/ci.yml`）：后端在 Postgres + Redis 服务容器上跑 pytest；前端跑 `tsc --noEmit` 与 `vite build`。push 到 main 与 PR 触发。

## 6. 部署方式 A：Docker Compose

```bash
cp .env.example .env      # 根目录，改密码与密钥
docker compose up -d --build
```

| 容器 | 端口 | 说明 |
|---|---|---|
| api | 8000 | 后端，启动时等待数据库就绪、建表、初始化管理员 |
| web | 18056 → 80 | Nginx 托管前端静态文件，`/api/` 反代到 api:8000，关闭缓冲以支持 SSE |
| postgres | 5432 | 数据卷 `pgdata` |
| redis | 6379 | 无密码 |
| minio | 9000 / 9001 | 数据卷 `miniodata`，9001 是控制台 |

- 访问 `http://<主机>:18056`，登录 `admin / admin123`，首次登录后改密码。
- 更新：`git pull && docker compose up -d --build`；有新迁移脚本时 `docker compose exec api python scripts/migrations/<文件>.py --apply`。
- 停止 `docker compose down`；连数据一起清 `docker compose down -v`（不可逆）。
- `docker-compose.yml` 里的 `version` 字段已废弃，compose v2 会告警，可删。

## 7. 部署方式 B：裸机 uvicorn + 外部数据服务

适合已有 PostgreSQL / Redis / MinIO 的机器。

```bash
cd <仓库>/backend
uv venv .venv --python 3.12 && uv pip install --python .venv/bin/python -r requirements.txt
# 写好 .env（DATABASE_URL 等指向外部服务，REDIS_URL 带密码）
setsid nohup .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 >> uvicorn.log 2>&1 < /dev/null &
curl -s http://127.0.0.1:8000/health
```

重启（更新代码后）：

```bash
cd <仓库> && git pull --ff-only
cd backend && pkill -f '[u]vicorn app.main'; sleep 2
setsid nohup .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 >> uvicorn.log 2>&1 < /dev/null &
tail -n 5 uvicorn.log
```

**通过 ssh 一行执行时，`pkill -f` 与启动命令必须分成两次执行**：`pkill -f` 按整条命令行匹配，
而 ssh 传过去的那条命令行里就含有 `uvicorn app.main:app` 这段文本，
写在一起会把执行命令的 shell 自己杀掉 —— 表现是服务停了、新进程没起来、ssh 返回 255。
模式里的 `[u]` 只能规避同一条命令里的 `pkill` 自身，规避不了后半段的启动命令。
前端 `vite preview` 同理。

nohup 方式机器重启后不会自动拉起。建议改成 systemd：

```ini
# /etc/systemd/system/agent-platform.service
[Unit]
Description=Agent Platform API
After=network.target

[Service]
WorkingDirectory=/path/to/agent-platform/backend
ExecStart=/path/to/agent-platform/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

`systemctl enable --now agent-platform`，日志走 `journalctl -u agent-platform -f`。

前端有两种托管方式：

- **有 nginx**：`npm run build` 后把 `frontend/dist` 交给 nginx，并把 `/api/` 反代到 8000，配置参考 `frontend/nginx.conf`（注意 `proxy_buffering off`，否则 SSE 会被攒包）。
- **零安装**：用 Vite 自带的预览服务器托管构建产物，它会沿用 `vite.config.ts` 里的 `^/api/` 代理：

```bash
cd <仓库>/frontend
npm run build
setsid nohup npx vite preview --port 18056 --host 0.0.0.0 --strictPort >> preview.log 2>&1 < /dev/null &
```

更新前端后重复上面三行即可（`package.json` 变了先 `npm ci`；先单独执行一次 `pkill -f "[v]ite preview"`，别和启动命令写在同一条 ssh 命令里，原因见上）。日志在 `frontend/preview.log`。
只要 18056 对外可达，后端 8000 就不需要开放公网：外部系统走 `http://<主机>:18056/api/v1/...` 同样能用 API Key 调用。

**来源 IP 与反代**：后端只在 `TRUSTED_PROXY_ENABLED=true` 时读取 `X-Real-IP`（优先）与 `X-Forwarded-For`（首项）判定客户端 IP，用于审计、IP 黑名单与按 IP 限流；关闭时一律取连接对端。开启的两个前提：① 8000 端口不能绕过反代直连（安全组只放行 18056），否则直连方可以伪造这两个头；② 反代必须用 `proxy_set_header` **覆写**这两个头而不是追加（`frontend/nginx.conf` 已这样写）。当前服务器用 vite preview 反代，它不设置这些头，保持默认 `false`，此时审计里记到的是 preview 进程的本机地址。

## 8. 日志与排障

### 8.1 日志

- 格式 `时间 级别 [request_id] 模块: 内容`，级别 INFO 起（`main.py` 的 `_configure_logging`）。
- 裸机方式在 `backend/uvicorn.log`；compose 方式 `docker compose logs -f api`；systemd 方式 `journalctl`。
- 每个请求一条访问日志：`app.access: GET /api/v1/agents 200 206ms`。
- 调度器触发、工作流失败、对话失败、上下文构建失败都带 `run_id`；线程池与调度线程里的日志 request_id 显示 `-`（contextvar 不跨线程）。

**按追踪 ID 排查**：任何错误响应都带 `trace_id`（也在响应头 `X-Request-Id`），前端 5xx 提示里直接显示。拿到后：

```bash
grep 1de7aa9bfeef4a18 backend/uvicorn.log
```

调用方也可以自己指定，便于把对方系统的调用链和本平台日志对齐：

```bash
curl -H 'X-Request-Id: caller-order-8823' http://127.0.0.1:8000/api/v1/agents -H "Authorization: Bearer $TOKEN"
```

只接受 `[A-Za-z0-9._-]{1,64}`，非法值会被忽略并另行生成，防止日志注入。

**查降级状态**：`GET /api/v1/system/status`（admin/developer）返回 `degraded` 数组，涵盖向量后端、登录限流、数据库、调度器注册数。
探活用 `GET /health`，其中 `embedding_mode=hash` 表示检索正在降级。

### 8.2 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 上传文档后状态 failed，错误含 `expected N dimensions` | `EMBEDDING_DIM` 与列维度不一致 | 查列维度：`SELECT atttypmod FROM pg_attribute WHERE attrelid='document_chunks'::regclass AND attname='embedding'`；改配置或走迁移改列后重新处理文档 |
| 检索结果明显只按字面匹配 | embedding 接口不可用或未配置，退化为 hash 向量 | 看 `/system/status` 的 `embedding` 与知识库页顶部告警；修好 `EMBEDDING_API_BASE / KEY` 后**重新上传文档**，旧切片仍是 hash 向量（切片 `meta.embedding_mode` 可区分） |
| 连续输错密码不锁定 | Redis 不可达或缺密码，限流失效 | 看 `/system/status` 的 `login_guard.enabled`；`REDIS_URL` 加密码后重启 |
| 定时任务到点不跑 | cron 表达式非法，注册时被跳过 | 看 `/system/status` 的 `scheduler`，`registered_jobs` 少于 `enabled_jobs` 即是；日志里有"cron 表达式非法" |
| 待审核的工作流重启后无法续跑 | checkpointer 在进程内存 | 重新运行；换 Postgres checkpointer 在演进路线 |
| 定时任务每次触发两条运行记录 | 两个后端进程连同一个库 | 只保留一个进程启用调度，或停用测试任务 |
| 对话报"模型不可用" | 模型被停用或智能体引用的模型被删 | 模型管理里检查 |
| 模型密钥解密失败 | `AES_KEY` 变了 | 恢复原 key；或重新录入所有模型密钥 |
| 前端 401 循环跳登录 | token 过期或 `SECRET_KEY` 变了 | 重新登录 |
| SSE 回复一次性整段出现 | 反向代理开了缓冲 | nginx `proxy_buffering off` |

### 8.3 快速自检脚本

```bash
cd backend && PYTHONPATH=. .venv/bin/python - <<'PY'
from sqlalchemy import text
from app.config import settings
from app.db.session import engine
with engine.connect() as c:
    dim = c.execute(text("SELECT atttypmod FROM pg_attribute WHERE attrelid='document_chunks'::regclass AND attname='embedding'")).scalar()
print("向量列维度", dim, "| 配置", settings.EMBEDDING_DIM, "| 一致", dim == settings.EMBEDDING_DIM)
import redis; print("Redis", redis.from_url(settings.REDIS_URL, socket_connect_timeout=3).ping())
from minio import Minio; print("MinIO 桶存在", Minio(settings.MINIO_ENDPOINT, access_key=settings.MINIO_ACCESS_KEY, secret_key=settings.MINIO_SECRET_KEY, secure=False).bucket_exists(settings.MINIO_BUCKET))
PY
```

## 9. 备份与恢复

- 数据库：`pg_dump -Fc agent_platform > backup.dump`；compose 下 `docker compose exec postgres pg_dump -U admin -Fc agent_platform`。恢复 `pg_restore -d agent_platform backup.dump`。
- 文档原文：MinIO 数据目录或桶级复制。
- 配置：`.env` 单独备份到仓库外，其中 `AES_KEY` 丢失等于丢失全部模型密钥。

## 10. 上线检查单

- [ ] `SECRET_KEY / AES_KEY / 数据库密码 / MinIO 凭据` 已改掉默认值
- [ ] 新迁移脚本已按顺序执行
- [ ] `EMBEDDING_DIM` 与库一致；embedding 接口可达或明确接受 hash 兜底
- [ ] `REDIS_URL` 可 ping
- [ ] 健康检查 200；登录成功；发一条对话
- [ ] 管理员默认密码已修改；生产关闭 `/docs`（演进路线项）
